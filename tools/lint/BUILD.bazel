# Lint tool configurations
# See linters.bzl for aspect definitions

exports_files([
    "golangci_lint.bzl",
    "lint.sh",
    "linters.bzl",
])

sh_binary(
    name = "lint",
    srcs = ["lint.sh"],
    visibility = ["//visibility:public"],
)
